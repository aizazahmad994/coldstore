<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.coldstore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.rec"></activity>
        <activity android:name=".Activities.UpdateActivity" />
        <activity android:name=".Activities.ViewActivityEntries$UpdateActivity" />
        <activity android:name=".Activities.ViewActivityEntries" />
        <activity android:name=".Activities.ViewActivity$ActivityView_entries" />
        <activity android:name=".Activities.ViewActivity" />
        <activity android:name=".Activities.addRecord" />
        <activity android:name=".Activities.search" />
        <activity android:name=".Activities.dashboard_munchi" />
        <activity android:name=".Activities.AdminDashBoardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.signup" />
        <activity android:name=".Activities.login" />
        <activity android:name=".Activities.MainActivity" />
    </application>

</manifest>